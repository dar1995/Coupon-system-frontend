import styled from "@emotion/styled";
import { MenuItem } from "@mui/material";

export const StyledMyAccountMI = styled(MenuItem)({
  "&:hover": {
    backgroundColor: "var(--color-secondary)",
  },
});
